apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${GITHUB_REPOSITORY}-deployment
  namespace: ${NAME_SPACE}
  labels:
    name: ${GITHUB_REPOSITORY}
    namespace: ${NAME_SPACE}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${GITHUB_REPOSITORY}
  template:
    metadata:
      labels:
        app: ${GITHUB_REPOSITORY}
    spec:
      containers:
        - name: ${GITHUB_REPOSITORY}
          image: ${IMAGE_NAME}
          ports:
            - containerPort: ${PORT}
          resources:
            limits:
              cpu: "1"
              memory: "500Mi"
            requests:
              cpu: "0.5"
              memory: "250Mi"